"use client";

/**
 * TawkChat component.
 * Custom socket events are disabled to ensure maximum widget stability
 * and prevent script crashes, ensuring the chat widget opens cleanly 
 * directly inside the same page/tab.
 */
export default function TawkChat() {
  return null; // The script injection itself is handled in layout.tsx
}
